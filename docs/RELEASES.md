# Releases

> Stub — filled in during the governance phase. Covers SemVer semantics per
> plugin, the `{plugin-name}--v{version}` tag convention, dependency-first
> release ordering, update flow, and rollback.
