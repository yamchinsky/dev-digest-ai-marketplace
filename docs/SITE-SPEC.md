# Site specification

> Stub — filled in before the catalog UI is built. Covers indexed fields,
> screens and routes, search and filter behavior, the plugin detail page, and
> the build/deploy pipeline.
