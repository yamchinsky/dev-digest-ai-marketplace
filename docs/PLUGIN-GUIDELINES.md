# Plugin guidelines

> Stub — filled in during the governance phase. Covers naming, required plugin
> structure, `plugin.json` fields, dependency declaration rules, path-variable
> conventions (`${CLAUDE_PLUGIN_ROOT}`, `${CLAUDE_SKILL_DIR}`), the no-hooks
> policy for v1 plugins, and the extraction editorial checklist.
